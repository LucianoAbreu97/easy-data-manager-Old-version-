import {StyleSheet} from 'react-native'
import config from '../../config'

const styleCriacao = StyleSheet.create({
  container: {
    flex: 1,
    textAlign: 'center',
    alignItems: 'center',
    backgroundColor: config.colors.primary
  }
})

export default styleCriacao